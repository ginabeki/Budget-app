<!-- app/views/groups/show.html.erb -->
<h1><%= @group.name %> Expenses</h1>

<ul>
  <% @expenses.each do |expense| %>
    <li>
      <%= expense.name %>
      <%= expense.amount %>
    </li>
  <% end %>
</ul>

<%= link_to "Add a new transaction", new_expense_path(group_id: @group.id) %>